#include <stdio.h>

int main(void) {
  printf("TODO: implement.\n");
  //TODO mmap the trusted.so
  //Parse the ELF format to know which segments we need to map.
  //Do the calls to the driver.
  return 0;
}
