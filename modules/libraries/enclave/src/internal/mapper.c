#include <linux/vmalloc.h>
#include "mapper.h"


/// Create the tree for the enclave.
int build_enclave_cr3(struct enclave_t* encl) {
 /* char* page = __vmalloc(byte_size, GFP_KERNEL, PAGE_SHARED	); 
  if (page == NULL) {
    return -1;
  }*/
  return 0;
}
